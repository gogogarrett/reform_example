<%= form_for @form, url: artists_path, method: :post do |f| %>

  <%= f.fields_for :songs, artist.songs do |art| %>
    <%= art.label :name %>
    <%= art.text_field :name %>
  <% end %>

  <%= f.submit %>
<% end %>
